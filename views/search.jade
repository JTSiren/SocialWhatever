extends layout

block content
  h1 I Can't Believe It's Not Twitter!
  h3 Search Results for " #{search} "

  ul.array
    each post in array
      li=  post.username + ' said - "' + post.content + '" at ' + post.date
